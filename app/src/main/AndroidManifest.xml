<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.adif.budidaya">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Budidaya Ikan"
        android:theme="@style/AppTheme">
        
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".KalkulatorActivity" />
        <activity android:name=".EstimasiPanenActivity" />
        <activity android:name=".BiayaProduksiActivity" />
        <activity android:name=".AnalisisKeuntunganActivity" />
        <activity android:name=".DatabaseActivity" />
        <activity android:name=".MonitoringActivity" />
        
    </application>

</manifest>